/**
 * Тип загружаемого файла.
 *
 * Поддерживаемые форматы:
 * - `image`: изображения для сообщений, постов и аватаров.
 * - `video`: видео-вложения.
 * - `audio`: аудио-вложения.
 * - `file`: файловые вложения.
 *
 * Значение `photo` отсутствует в актуальной схеме; используйте `image`.
 */
export type UploadType = 'image' | 'video' | 'audio' | 'file';
